#include <string>

#ifndef EXPRESSION_ERROR_H
#define EXPRESSION_ERROR_H "expressionError.h"

bool expressionError (std::string &);

const std::string invalidExpression = "Invalid expression";

#endif
